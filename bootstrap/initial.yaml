apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  creationTimestamp: null
  name: root
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/Wompipomp/argocd-test-repo
        revision: HEAD
        directories:
          - path: "team1/bootstrap/*.yaml"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: '{{path[1]}}'
      name: 'dev-{{path[1]}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/Wompipomp/argocd-test-repo
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '*'
      ignoreDifferences:
        - group: argoproj.io
          jsonPointers:
            - /status
          kind: Application
      syncPolicy:
        automated:
          allowEmpty: true
          prune: true
          selfHeal: false